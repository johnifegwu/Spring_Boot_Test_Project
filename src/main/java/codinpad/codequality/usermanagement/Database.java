//Package name was not set previously.
package codinpad.codequality.usermanagement;

//The name Database should be changed to UserManagementDB
class Database {
    public boolean isConnected() {
        return true;
    }
}